import {BaseUserSet} from "./BaseUserSet";

export class UserSocketSet extends BaseUserSet {

}

const userSocketSet = new UserSocketSet();

module.exports = userSocketSet;

export default userSocketSet;
