import {BaseUserSet} from "./BaseUserSet";

export class SubmissionSet extends BaseUserSet{
}

const submissionSet = new SubmissionSet();

module.exports = submissionSet;

export default submissionSet;
